import { Config } from 'tailwindcss';

interface LoadConfigOptions {
    cwd: string;
    config: string;
    moduleName: string;
}
type LoadConfigResult = null | {
    filepath: string;
    config: Config;
    isEmpty?: boolean;
};
declare function loadConfig(options?: Partial<LoadConfigOptions>): Promise<LoadConfigResult>;

export { type LoadConfigOptions, type LoadConfigResult, loadConfig };
