import { findDOM } from '../../utils/index.js';
import { LivePlayerContext } from './LivePlayerContext.js';

/**
 * 创建 live-player 上下文 LivePlayerContext 对象
 *
 * @canUse createLivePlayerContext
 */
const createLivePlayerContext = (id, inst) => {
    const el = findDOM(inst);
    const LivePlayer = el === null || el === void 0 ? void 0 : el.querySelector(`taro-live-player-core[id=${id}]`);
    const context = new LivePlayerContext(LivePlayer);
    return context;
};

export { createLivePlayerContext };
//# sourceMappingURL=index.js.map
