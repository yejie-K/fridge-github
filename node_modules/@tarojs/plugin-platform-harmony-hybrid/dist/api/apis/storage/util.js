function displayExecRes(status, method) {
    if (!(status === null || status === void 0 ? void 0 : status.done)) {
        console.error({ errMsg: `${method} execution fail: ` + (status === null || status === void 0 ? void 0 : status.errorMsg) });
    }
    else {
        /* empty */
    }
}
function handleData(data) {
    const type = typeof data;
    let obj = {};
    if (type === 'symbol') {
        obj = { data: '' };
    }
    else {
        obj = { data };
    }
    return obj;
}

export { displayExecRes, handleData };
//# sourceMappingURL=util.js.map
