/**
 * 拉取 backgroundFetch 客户端缓存数据
 *
 * @canUse getBackgroundFetchData
 * @null_implementation
 */
const getBackgroundFetchData = function (option) {
    const name = 'getBackgroundFetchData';
    return new Promise(() => {
        var _a, _b;
        try {
            (_a = option === null || option === void 0 ? void 0 : option.fail) === null || _a === void 0 ? void 0 : _a.call(option, { errMsg: `${name}:fail` });
        }
        finally {
            (_b = option === null || option === void 0 ? void 0 : option.complete) === null || _b === void 0 ? void 0 : _b.call(option, { errMsg: `ok` });
        }
    });
};

export { getBackgroundFetchData };
//# sourceMappingURL=getBackgroundFetchData.js.map
