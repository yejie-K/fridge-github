import { shouldBeFunction } from '../utils/index.js';
import nativeLocation from './NativeLocation.js';

/**
 * 取消监听持续定位接口返回失败时触发
 *
 * @canUse offLocationChangeError
 */
const offLocationChangeError = (callback) => {
    const name = 'offLocationChangeError';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    nativeLocation.offLocationChangeError(callback);
};

export { offLocationChangeError };
//# sourceMappingURL=offLocationChangeError.js.map
