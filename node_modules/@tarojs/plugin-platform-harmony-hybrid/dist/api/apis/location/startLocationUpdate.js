import { shouldBeObject } from '../utils/index.js';
import { MethodHandler } from '../utils/handler.js';
import nativeLocation from './NativeLocation.js';

/**
 * 开启小程序进入前台时接收位置消息
 *
 * @canUse startLocationUpdate
 */
const startLocationUpdate = (options) => {
    const name = 'startLocationUpdate';
    const isValid = shouldBeObject(options).flag || typeof options === 'undefined';
    if (!isValid) {
        const res = { errMsg: `${name}:fail invalid params` };
        console.error(res.errMsg);
        return;
    }
    const { success, fail, complete } = options || {};
    const handle = new MethodHandler({ name, success, fail, complete });
    nativeLocation.startLocationUpdate({
        success: (res) => {
            handle.success(res);
        },
        fail: (res) => {
            handle.fail(res).catch((err) => {
                console.error(err);
            });
        },
    });
};

export { startLocationUpdate };
//# sourceMappingURL=startLocationUpdate.js.map
