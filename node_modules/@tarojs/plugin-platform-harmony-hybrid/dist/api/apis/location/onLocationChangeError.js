import { shouldBeFunction } from '../utils/index.js';
import nativeLocation from './NativeLocation.js';

/**
 * 监听持续定位接口返回失败时触发
 *
 * @canUse onLocationChangeError
 * @__callback [errCode]
 */
const onLocationChangeError = (callback) => {
    const name = 'onLocationChangeError';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    nativeLocation.onLocationChangeError(callback);
};

export { onLocationChangeError };
//# sourceMappingURL=onLocationChangeError.js.map
