import native from '../NativeApi.js';
export { requestSubscribeDeviceMessage } from '@tarojs/taro-h5';

/**
 * 请求订阅消息
 *
 * @canUse requestSubscribeMessage
 * @__object [tmplIds]
 * @__success [errMsg, TEMPLATE_ID]
 */
const requestSubscribeMessage = (options) => {
    var _a, _b;
    try {
        native.requestSubscribeMessage(options);
    }
    catch (res) {
        const result = {
            errCode: 500,
            errMsg: JSON.stringify(res),
        };
        (_a = options.fail) === null || _a === void 0 ? void 0 : _a.call(options, result);
        (_b = options.complete) === null || _b === void 0 ? void 0 : _b.call(options, result);
    }
};

export { requestSubscribeMessage };
//# sourceMappingURL=subscribe-message.js.map
