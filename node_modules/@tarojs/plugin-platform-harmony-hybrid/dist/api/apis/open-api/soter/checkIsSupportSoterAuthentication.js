import native from '../../NativeApi.js';
import { shouldBeObject } from '../../utils/index.js';
import { MethodHandler } from '../../utils/handler.js';

/**
 * 获取本机支持的 SOTER 生物认证方式
 *
 * @canUse checkIsSupportSoterAuthentication
 * @__success [supportMode]
 */
const checkIsSupportSoterAuthentication = (options) => {
    const name = 'checkIsSupportSoterAuthentication';
    // options must be an Object
    const isObject = shouldBeObject(options);
    if (!isObject.flag) {
        const res = { errMsg: `${name}:fail ${isObject.msg}` };
        console.error(res.errMsg);
        return Promise.reject(res);
    }
    const { success, fail, complete } = options;
    const handle = new MethodHandler({ name, success, fail, complete });
    return new Promise((resolve, reject) => {
        native.checkIsSupportSoterAuthentication({
            success: (res) => {
                handle.success(res, { resolve, reject });
            },
            fail: (err) => {
                handle.fail(err, { resolve, reject });
            },
        });
    });
};

export { checkIsSupportSoterAuthentication };
//# sourceMappingURL=checkIsSupportSoterAuthentication.js.map
