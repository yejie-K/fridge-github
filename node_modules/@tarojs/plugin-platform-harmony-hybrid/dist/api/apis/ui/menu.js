import native from '../NativeApi.js';

/**
 * 获取菜单按钮（右上角胶囊按钮）的布局位置信息
 *
 * @canUse getMenuButtonBoundingClientRect
 * @__return [left, top, right, bottom, height, width]
 */
const getMenuButtonBoundingClientRect = () => {
    const data = native.getMenuButtonBoundingClientRect();
    const rect = JSON.parse(JSON.stringify(data));
    return {
        left: rect.left,
        top: rect.top,
        right: rect.right,
        bottom: rect.bottom,
        height: rect.height,
        width: rect.width,
    };
};

export { getMenuButtonBoundingClientRect };
//# sourceMappingURL=menu.js.map
