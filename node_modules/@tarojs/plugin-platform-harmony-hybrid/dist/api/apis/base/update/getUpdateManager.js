import { NativeUpdateManager } from '../../interface/NativeUpdateManager.js';

/**
 * 获取全局唯一的版本更新管理器
 *
 * @canUse getUpdateManager
 */
const getUpdateManager = () => {
    // 使用native方法
    return NativeUpdateManager.getUpdateManager();
};

export { getUpdateManager };
//# sourceMappingURL=getUpdateManager.js.map
