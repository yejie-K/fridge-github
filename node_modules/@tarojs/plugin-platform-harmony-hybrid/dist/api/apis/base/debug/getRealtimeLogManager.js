/**
 * 获取实时日志管理器对象
 *
 * @canUse getRealtimeLogManager
 * @null_implementation
 */
const getRealtimeLogManager = () => {
    return new RealtimeLogManager();
};
/**
 * 实时日志管理器
 *
 * @canUse RealtimeLogManager
 * @null_implementation
 */
class RealtimeLogManager {
    addFilterMsg() {
    }
    error() {
    }
    in() {
    }
    info() {
    }
    setFilterMsg() {
    }
    tag(tagName) {
        return new RealtimeTagLogManager(tagName);
    }
    warn() {
    }
}
/**
 * 给定标签的实时日志管理器
 *
 * @canUse RealtimeTagLogManager
 * @null_implementation
 */
class RealtimeTagLogManager {
    constructor(tagName) {
        this._tagName = tagName;
    }
    addFilterMsg(_msg) {
    }
    error(_key, _value) {
    }
    info(_key, _value) {
    }
    setFilterMsg(_msg) {
    }
    warn(_key, _value) {
    }
}

export { getRealtimeLogManager };
//# sourceMappingURL=getRealtimeLogManager.js.map
