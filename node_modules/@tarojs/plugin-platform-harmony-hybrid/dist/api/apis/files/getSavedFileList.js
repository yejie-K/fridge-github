import { shouldBeObject } from '../utils/index.js';
import { MethodHandler } from '../utils/handler.js';
import native from '../NativeApi.js';

/**
 * 获取本地已保存的文件列表
 *
 * @canUse getSavedFileList
 * @__success [fileList]
 */
const getSavedFileList = (options) => {
    const name = 'getSavedFileList';
    // options must be an Object
    const isValid = shouldBeObject(options).flag || typeof options === 'undefined';
    if (!isValid) {
        const res = { errMsg: `${name}:fail invalid params` };
        console.error(res.errMsg);
        return Promise.reject(res);
    }
    const { success, fail, complete } = options;
    const handle = new MethodHandler({ name, success, fail, complete });
    return new Promise((resolve, reject) => {
        native.getSavedFileList({
            success: (res) => {
                const result = {
                    fileList: res.fileList,
                    errMsg: res.errMsg,
                };
                handle.success(result, { resolve, reject });
            },
            fail: (err) => {
                handle.fail(err, { resolve, reject });
            },
        });
    });
};

export { getSavedFileList };
//# sourceMappingURL=getSavedFileList.js.map
