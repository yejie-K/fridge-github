import { shouldBeFunction } from '../../utils/index.js';
import native from '../../NativeApi.js';

/**
 * 监听低功耗蓝牙连接状态的改变事件
 *
 * @canUse onBLEConnectionStateChange
 * @__callback [connected, deviceId]
 */
const onBLEConnectionStateChange = (callback) => {
    const name = 'onBLEConnectionStateChange';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    native.onBLEConnectionStateChange(callback);
};

export { onBLEConnectionStateChange };
//# sourceMappingURL=onBLEConnectionStateChange.js.map
