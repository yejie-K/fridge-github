import { shouldBeFunction } from '../../utils/index.js';
import native from '../../NativeApi.js';

/**
 * 取消监听键盘高度变化事件
 *
 * @canUse offKeyboardHeightChange
 */
const offKeyboardHeightChange = (callback) => {
    const name = 'offKeyboardHeightChange';
    const isValid = shouldBeFunction(callback).flag || typeof callback === 'undefined';
    if (!isValid) {
        const res = { errMsg: `${name}:fail invalid params` };
        console.error(res.errMsg);
        return;
    }
    // @ts-ignore
    native.offKeyboardHeightChange(callback);
};

export { offKeyboardHeightChange };
//# sourceMappingURL=offKeyboardHeightChange.js.map
