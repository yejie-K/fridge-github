import { shouldBeFunction } from '../../utils/index.js';
import nativeWifi from './NativeWifi.js';

/**
 * 取消监听连接上 Wi-Fi 的事件
 *
 * @canUse offWifiConnectedWithPartialInfo
 */
const offWifiConnectedWithPartialInfo = (callback) => {
    const name = 'offWifiConnectedWithPartialInfo';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    nativeWifi.offWifiConnectedWithPartialInfo((res) => {
        const result = {
            /** 只包含 SSID 属性的 WifiInfo 对象 */
            wifi: { SSID: res.wifi.SSID },
        };
        callback && callback(result);
    });
};

export { offWifiConnectedWithPartialInfo };
//# sourceMappingURL=offWifiConnectedWithPartialInfo.js.map
