import { shouldBeFunction } from '../../utils/index.js';
import nativeWifi from './NativeWifi.js';

/**
 * 取消监听获取到 Wi-Fi 列表数据事件
 *
 * @canUse offGetWifiList
 */
const offGetWifiList = (callback) => {
    const name = 'offGetWifiList';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    nativeWifi.offGetWifiList(callback);
};

export { offGetWifiList };
//# sourceMappingURL=offGetWifiList.js.map
