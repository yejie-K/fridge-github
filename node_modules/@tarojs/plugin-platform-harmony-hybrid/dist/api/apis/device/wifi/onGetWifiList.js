import { shouldBeFunction } from '../../utils/index.js';
import nativeWifi from './NativeWifi.js';

/**
 * 监听获取到 Wi-Fi 列表数据事件
 *
 * @canUse onGetWifiList
 * @__callback [wifiList]
 */
const onGetWifiList = (callback) => {
    const name = 'onGetWifiList';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    nativeWifi.onGetWifiList(callback);
};

export { onGetWifiList };
//# sourceMappingURL=onGetWifiList.js.map
