import { shouldBeFunction } from '../../utils/index.js';
import native from '../../NativeApi.js';

/**
 * 取消监听蓝牙适配器状态变化事件
 *
 * @canUse offBluetoothAdapterStateChange
 */
const offBluetoothAdapterStateChange = (callback) => {
    const name = 'offBluetoothAdapterStateChange';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    native.offBluetoothAdapterStateChange(callback);
};

export { offBluetoothAdapterStateChange };
//# sourceMappingURL=offBluetoothAdapterStateChange.js.map
