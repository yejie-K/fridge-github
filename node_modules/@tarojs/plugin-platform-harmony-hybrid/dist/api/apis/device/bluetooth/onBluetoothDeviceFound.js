import { shouldBeFunction } from '../../utils/index.js';
import native from '../../NativeApi.js';

/**
 * 监听寻找到新设备的事件
 *
 * @canUse onBluetoothDeviceFound
 * @__callback [devices]
 */
const onBluetoothDeviceFound = (callback) => {
    const name = 'onBluetoothDeviceFound';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    native.onBluetoothDeviceFound((res) => {
        const result = {
            /** 新搜索到的设备列表 */
            devices: res,
        };
        callback(result);
    });
};

export { onBluetoothDeviceFound };
//# sourceMappingURL=onBluetoothDeviceFound.js.map
