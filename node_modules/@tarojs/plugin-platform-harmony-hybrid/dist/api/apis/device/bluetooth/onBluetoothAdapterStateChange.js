import { shouldBeFunction } from '../../utils/index.js';
import native from '../../NativeApi.js';

/**
 * 监听蓝牙适配器状态变化事件
 *
 * @canUse onBluetoothAdapterStateChange
 * @__callback [available, discovering]
 */
const onBluetoothAdapterStateChange = (callback) => {
    const name = 'onBluetoothAdapterStateChange';
    // callback must be an Function
    const isFunction = shouldBeFunction(callback);
    if (!isFunction.flag) {
        const res = { errMsg: `${name}:fail ${isFunction.msg}` };
        console.error(res.errMsg);
        return;
    }
    native.onBluetoothAdapterStateChange(callback);
};

export { onBluetoothAdapterStateChange };
//# sourceMappingURL=onBluetoothAdapterStateChange.js.map
